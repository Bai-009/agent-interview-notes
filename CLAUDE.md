# 仓库管家协议

> 本文件定义 Claude 在本仓库中的固定职责与作业流程。**每次会话开始先读这里。**
> 用户的原话：「你的职责就是做好这里面的管家，把每一份内容精心有逻辑的整理起来。」

---

## 1. 这是什么

一份 AI Agent 方向面试题库（**22 题 / 6 模块**）的**攻题过程复盘记录**。

**它不是答案集，是过程记录。** 差异化价值在于保留了「裸答 → 诊断 → 生产级答案」的完整落差——标准答案到处都有，真实的思考落差没有。

**终局目标**：整理成 GitHub 上的公开学习仓库（见 [上线清单](00-索引/GitHub上线清单.md)）。

---

## 2. 用户会做什么 / 我做什么

**用户**：把攻题过程原样贴过来——题目、他的裸答、对方的反馈、杀手句。可能一次一题，也可能一次贴好几题；格式不固定，可能有断档（比如跳过了某题）。

**我（管家）**：把它整理成结构化记录并维护全仓索引。**用户不需要指定放哪、编几号、更新哪些文件——那是我的活。**

---

## 3. 标准作业流程（每次收到新内容照做）

1. **辨识边界**：这次贴的覆盖哪几题？哪些是「已完成」（有裸答+反馈+杀手句）、哪些是「进行中」（只有题目）、哪些只是路过提及。
2. **归档原文** → `_原始记录/YYYY-MM-DD-<范围>.md`，**一字不改**。
3. **定位归属**：按题库原始题号定编号（第三题 = `Q03`），放进对应模块目录。**编号只跟题库走，不自己发明。**
4. **写记录文件** → `<模块目录>/Qxx-<短标题>.md`，套第 4 节的规范结构。
5. **同步四处索引**（一处都不能漏）：
   - `00-索引/题库进度.md` —— 掌握度、链接、**跨题公共资产表**
   - `00-索引/复习清单.md` —— 到期排期、分组
   - `00-索引/高频追问速查.md` —— 抽取可迁移的追问模式
   - `README.md` —— 顶部进度数字 + 题目索引表
6. **git 提交**，中文 message，说清这次覆盖了哪几题、掌握度如何。

---

## 4. 记录文件的规范结构

固定十节（模板见 [`_模板/题目模板.md`](_模板/题目模板.md)，范本见 [Q04](02-模型输出控制/Q04-幻觉成因与治理.md)）：

| 节 | 内容 | 硬性要求 |
| --- | --- | --- |
| 头部 | 徽章行 + 判定结论 | 模块/难度/掌握度/首答/复习次数 |
| **⚡30秒速览** | 一句话回答 / 必说关键词 / 上次踩的坑 / 答题骨架 | **必须在最顶部**——复习时只看这块 |
| 1 | 题目原文 + 考察意图 | |
| 2 | **我的原始回答** | **一字不改，不美化** |
| 3 | 差距诊断（四维表 + 最致命的一条） | 必须点出「最致命的一条」 |
| 4 | 完整答案（一句话定义 → 分层展开 → 加分项） | 对应「及格线 + 生产级」两步 |
| 5 | 第一性原理 | 本质约束 / 不这么做会怎样 |
| 6 | 追问链（表格 + 自查勾选框） | |
| 7 | 待查清单 | 用户自己挂起的问题，**必须保留** |
| 8 | 关联（`[[Qxx-...]]` 双链） | 链到具体题号，不泛泛 |
| **9** | **杀手句** | **必须有**，面试里能直接说出口的一句 |
| 10 | 复习记录表 | |

未作答的题只建**题目档**（题目+提示+作答自查+空白作答区），编号后缀状态标 `🔄 进行中`。

---

## 5. 写作原则（这几条决定质量）

- **裸答一字不改。** 差距诊断是全仓价值所在，美化掉裸答就失去了复盘基准线。
- **按内容判定，不按用户自评。** 用户说「我表述不好」时往往已经答得很深（见 Q04）。别跟着自评走。
- **肯定要具体。** 说清答对的部分**为什么**对、比通常水位高在哪，不要泛泛地夸。
- **每个治理/方法必须指名道姓对应某一层的病。** 「列了一堆手段」是八股水位，「手段与成因逐层咬合」才是生产级。
- **保留并鼓励「我不知道 + 我会去查什么」。** 已确认这是面试加分动作，写进待查清单，不要替他补全。
- **不预先给出未作答题的答案。** 出题人给了提示的，只记录提示。答案的价值在于用户自己推出来。
- **跨题复用要标出来。** 某题推出的结论若会在其他题复用，登记进「跨题公共资产表」并在两边加双链。

---

## 6. 对话协议（用户攻题时的四步走）

裸答（60秒版）→ 及格线（题库标准答案核心）→ **生产级那一层** → **一句杀手句**。

三条规矩：
1. 每题四步走
2. **把题库当靶子，不当圣经**——标准答案是八股水位，目标是每题都答出题库没有的那一层
3. 每次两三题，对话式，**不赶进度**

已答透的题可「协议升级」：跳过及格线，直接要生产级完整答案，只做验收。

---

## 7. 命名与约定

- 记录文件：`Qxx-<短标题>.md`，`xx` 两位、对齐题库原题号、**全局唯一只增不减**
- 原始归档：`_原始记录/YYYY-MM-DD-<范围>.md`
- 掌握度：🟢 已掌握（能主动展开+扛住追问）｜🟡 待巩固（能答但不完整）｜🔴 未过关｜⬜ 未作答｜🔄 进行中
- 复习间隔：首答 → +1天 → +3天 → +7天 → +21天 → +60天；🟢 跳一档，🔴 退回 +1 天
- 日期一律写绝对日期，不写「昨天」「上周」
